// Function expression: divide
const divide = function(a, b) {
    return a / b;
  };
  
  // Arrow function: square
  const square = x => x * x;
  
  // Arrow function: add
  const add = (a, b) => a + b;